<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Blog</title>
  
  
  <link href="http://example.com/atom.xml" rel="self"/>
  
  <link href="http://example.com/"/>
  <updated>2021-03-18T06:16:33.525Z</updated>
  <id>http://example.com/</id>
  
  <author>
    <name>lw384</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>Promise A+规范-译文</title>
    <link href="http://example.com/2021/03/18/Promise-A-%E8%A7%84%E8%8C%83-%E8%AF%91%E6%96%87/"/>
    <id>http://example.com/2021/03/18/Promise-A-%E8%A7%84%E8%8C%83-%E8%AF%91%E6%96%87/</id>
    <published>2021-03-18T06:05:04.000Z</published>
    <updated>2021-03-18T06:16:33.525Z</updated>
    
    <content type="html"><![CDATA[<h1 id="Promises-A"><a href="#Promises-A" class="headerlink" title="Promises/A+"></a>Promises/A+</h1><p><strong>一个开源的、健全的、可操作的JavaScript Promises规范，由开发者制定，为其他开发者提供参考</strong></p><p><code>Promise</code>代表一个异步操作的最终结果。与之进行交互的主要方法是通过它的<code>then</code>方法，该方法注册回调函数接受<code>Promise</code>的最终结果或<code>Promise</code>未完成的原因。</p><p>本规范详细说明了<code>then</code>方法的执行过程，所有遵循Promise/A+实现的promise均可以本规范作为参照。因此本规范是十分稳定的。虽然Promises/A+组织有时会通过向后兼容的小改动来修订这份规范，用于处理一些新发现的边界情况，但是只有经过仔细的思考、讨论与测试后，我们才会进行大规模不向下兼容的更新。</p><p>从历史上看，Promises/A+把较早的Promise/A提案的建议明确为行为标准，扩展原有约定俗成的行为，并忽略了未特指的情况与有问题的情况。</p><p>最后，Promise/A+规范的核心不涉及如何去创建、解决或拒绝Promise,取而代之选择聚焦于如何提供一个可操作的<code>then</code>方法。未来在相关规范的工作中可能会涉及这些话题。</p><h2 id="1-术语"><a href="#1-术语" class="headerlink" title="1. 术语"></a>1. 术语</h2><h3 id="1-1-promise"><a href="#1-1-promise" class="headerlink" title="1.1 promise"></a>1.1 promise</h3><p>promise 是定义了行为符合本规范的<code>then</code>方法的对象或者函数。</p><h3 id="1-2-thenable"><a href="#1-2-thenable" class="headerlink" title="1.2 thenable"></a>1.2 thenable</h3><p>thenable 表示定义了<code>then</code>方法的对象或函数。</p><h3 id="1-3-value"><a href="#1-3-value" class="headerlink" title="1.3 value"></a>1.3 value</h3><p>value 指任何JavaScript合法值（包括undefined，thenable和promise）。</p><h3 id="1-4-exception"><a href="#1-4-exception" class="headerlink" title="1.4 exception"></a>1.4 exception</h3><p>exception 是使用<code>throw</code>语句抛出的异常值。</p><h3 id="1-5-reason"><a href="#1-5-reason" class="headerlink" title="1.5 reason"></a>1.5 reason</h3><p>reason 表示一个promise拒绝的原因。</p><h2 id="2-要求"><a href="#2-要求" class="headerlink" title="2.要求"></a>2.要求</h2><h3 id="2-1-Promise的状态"><a href="#2-1-Promise的状态" class="headerlink" title="2.1 Promise的状态"></a>2.1 Promise的状态</h3><p>一个promise的当前状态必须是以下三种中的一种：pending(等待态) ，fulfilled（完成态） 或 rejected（拒绝态）</p><p>2.1.1 当promise状态为pending时：</p><p>​        2.1.1.1.  可能转变为fulfilled或rejected。</p><p>2.1.2 当promise状态为fulfilled时：</p><p>​         2.1.2.1 不会转变为其他状态</p><p>​         2.1.2.2 必须有一个确定的值，不会再改变</p><p>2.1.3 当promise状态为rejected时，</p><p>​         2.1.3.1 不会再变为其他状态</p><p>​         2.1.3.2 必须返回确定的<code>reason</code>，不会再改变</p><p>这里的不可变指的是恒等（即可用 <code>===</code> 判断相等），而不是意味着更深层次的不可变（<strong>注：</strong>指当 value 或 reason 不是基本值时，只要求其引用地址相等，但属性值可被更改）。</p><h3 id="2-2-then方法"><a href="#2-2-then方法" class="headerlink" title="2.2 then方法"></a>2.2 then方法</h3><p>一个promise必须提供一个then方法来获取当前值，终值以及拒绝原因。</p><p>promise的then方法接受两个参数:</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">promise.then(onFulfilled, onRejected)</span><br></pre></td></tr></table></figure><p>2.2.1 参数可选</p><p><code>onFulfilled</code>和<code>onRejected</code>都是可选的参数：</p><p>​        2.2.1.1 如果<code>onFulfilled</code>不是一个函数，则会被忽略                       </p><p>​        2.2.1.1 如果<code>onRejected</code>不是一个函数，则会被忽略</p><p>2.2.2  onFulfilled</p><p><code>onFulfilled</code>是函数：</p><p>​       2.2.2.1 在promise状态变为fulfilled时被调用，promise的返回值作为其第一个入参</p><p>​       2.2.2.2 在promise的状态变为fulfilled前不可被调用</p><p>​       2.2.2.3 仅可以被回调一次</p><p>2.2.3  onRejected</p><p><code>onRejected</code>是函数：</p><p>​        2.2.3.1 在promise状态变为rejected时被调用，promise的拒绝reason作为其第一个入参</p><p>​       2.2.3.2 在promise的状态变为rejected前不可被调用</p><p>​       2.2.3.3 仅可以被回调一次</p><p>2.2.4 调用时机</p><p><code>onFulfilled</code> 和 <code>onRejected</code> 只有在<a href="http://es5.github.io/#x10.3">执行环境</a>堆栈仅包含”platform code”时才可被调用[^3.1] </p><p>2.2.5  调用要求</p><p><code>onFulfilled</code> 与 <code>onRejected</code> 必须以函数形式被回调（即没有this）[^3.2]</p><p>2.2.6 多次调用</p><p>then方法可以在同一promise中多次被调用</p><p>​        2.2.6.1 如果promise的状态为fulfilled，每个<code>onFulfilled</code> 都必须按照他们的注册顺序依次回调。</p><p>​       2.2.6.2 如果promise的状态为rejected，每个<code>onRejected</code> 都必须按照他们的注册顺序依次回调。</p><p>2.2.7 then返回</p><p>then方法必须返回一个promise对象[^3.3]</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">promise2 = promise1.then(onFulfilled, onRejected);</span><br></pre></td></tr></table></figure><p>​          2.2.7.1 如果<code>onFulfilled</code> 或<code>onRejected</code> 返回值x，执行Promise解决程序<code>[[Resolve]](promise2,x)</code>。</p><p>​         2.2.7.2 如果<code>onFulfilled</code> 或<code>onRejected</code> 抛出异常 <code>e</code>,promise必须拒绝执行，并以<code>e</code>为reason。</p><p>​         2.2.7.3 如果 <code>onFulfilled</code> 不是一个函数并且promise1的状态为fulfilled，promise2必须成功执行，并返回与promise1相同的值。</p><p>​       2.2.7.4 如果 <code>onRejected</code> 不是一个函数并且promise2的状态为rejected，promise2必须拒绝执行，并返回与promise1相同的reason。</p><h3 id="2-3-Promise解决过程"><a href="#2-3-Promise解决过程" class="headerlink" title="2.3 Promise解决过程"></a>2.3 Promise解决过程</h3><p>promise解决过程是一个抽象的操作，以一个promise和值作为输入，表示为<code>[[Resolve]](promise,x)</code>。如果<code>x</code>具有then方法且看上去像一个promise，解决程序则会使promise接受x的状态。否则，用 <code>x</code> 的值来执行 <code>promise</code>。</p><p>这种thenable的特性使得promise的实现更具有通用性，只要其暴露一个符合Promise/A+规范的then方法。同时也使得遵循Promise/A+规范的实现与不太规范的实现能兼容。</p><p>运行 <code>[[Resolve]](promise, x)</code> 需遵循以下步骤：</p><p>2.3.1 x与promise相同</p><p>如果Promise与x指向同一对象，以TypeError为理由拒绝执行</p><p>2.3.2 x是promise</p><p>如果x 是promise，则使 <code>promise</code> 接受 <code>x</code> 的状态 [^3.4]</p><p>​       2.3.2.1 如果x处于pending状态，promise必须保持pending状态直到x状态变为fulfilled或rejected。</p><p>​       2.3.2.2 如果x处于fulfilled状态，fulfilled状态的promise必须有同样的值</p><p>​       2.3.2.3 如果x处于rejected状态，rejected状态的promise必须有相同的reason</p><p>2.3.3 x是对象或者函数</p><p>如果x是一个对象或者函数</p><p>​      2.3.3.1 将<code>x.then</code> 赋值给 <code>then</code>[^3.5]</p><p>​      2.3.3.2  如果取<code>x.then</code>的值时抛出异常<code>e</code>,以<code>e</code>为原因拒绝promise</p><p>​      2.3.3.3  如果<code>then</code>是一个函数，以<code>x</code>作为<code>this</code>调用，<code>then</code>方法的参数为两个回调函数，第一个参数是<code>resolvePromise</code>，第二个参数是<code>rejectPromise</code>：</p><p>​            2.3.3.3.1 当使用<code>y</code>值调用<code>resolvePromise</code>，运行<code>[[Resolve]](promise, y)</code></p><p>​            2.3.3.3.2  当使用<code>r</code>为原因调用<code>rejectPromise</code>，以<code>r</code>为拒因拒绝promise</p><p>​            2.3.3.3.3  如果同时调用<code>resolvePromise</code>和<code>rejectPromise</code>或对同一个参数进行了多次调用，则优先第一个调用优先，其他任何调用都将被忽略。</p><p>​            2.3.3.3.4 如果<code>then</code>方法抛出了异常<code>e</code></p><p>​                    2.3.3.3.4.1 如果已调用<code>resolvePromise</code>或<code>rejectPromise</code>，则忽略。</p><p>​                    2.3.3.3.4.2 否则，以<code>e</code>为原因拒绝promise</p><p>​      2.3.3.4 如果<code>then</code>方法不是函数，则以<code>x</code>为参数执行promise</p><p>2.3.4  <code>x</code>不是函数或对象，则以<code>x</code>为参数执行promise</p><p>如果promise被一个循环的thenable链中的thenable对象执行，而因为<code>[[Resolve]](promise, y)</code>的递归属性，使得<code>[[Resolve]](promise, y)</code>被再次调用，根据上述算法产生无限递归的情况。算法鼓励但不强制要求去检测这种递归情况是否存在，如果检测到这种情况存在，则以TypeError为原因拒绝执行[^3.6]。</p><h2 id="3-注释"><a href="#3-注释" class="headerlink" title="3 注释"></a>3 注释</h2><p>[^3.1]: “plantform code”指的是引擎、环境、promise实施代码。实际中确保<code>onFulfilled</code>和<code>onRejected</code>异步执行，在then方法被调用的那一轮事件调用结束后使用新的栈。可以使用“macro-task”机制例如<code>setTimeout</code>或<code>setImmediate</code>或使用“micro-task”机制例如<code>MutationObserver</code>或<code>process.nextTick</code>执行。由于 promise 的实施代码本身就是”plantform code”（<strong>注：</strong>即都是 JavaScript），故代码自身在处理在处理程序时可能已经包含一个任务调度队列。<br>[^3.2]: 在严格模式下，<code>this</code>的值为<code>undefined</code>;在非严格模式下，<code>this</code>为全局对象。<br>[^3.3]: 代码实现在满足所有要求的情况下可以允许 <code>promise2 === promise1</code> 。每个实现都要说明其是否允许以及在何种条件下允许 <code>promise2 === promise1</code> 。<br>[^3.4]: 通常，若 <code>x</code> 符合当前实现，我们才认为它是真正的 <em>promise</em> 。这一规则允许那些特例实现采用符合已知要求的 Promises 状态。<br>[^3.5]: 该过程我们先存储了<code>x.then</code>的引用，然后测试这个引用，再调用该引用，避免了对x.then属性的多次访问。这种预防措施对于保护访问者属性的一致性非常重要，因为访问者属性值在两次检索期间可能发生变化。<br>[^3.6]: 实现不应该对thenable链进行深度的限制，并且假设超出深度限制的就是无限递归。只有真正的无限递归会抛出<code>TypeError</code>;如果是一条无限长的链上 <em>thenable</em> 均不相同，那么递归下去永远是正确的行为。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;Promises-A&quot;&gt;&lt;a href=&quot;#Promises-A&quot; class=&quot;headerlink&quot; title=&quot;Promises/A+&quot;&gt;&lt;/a&gt;Promises/A+&lt;/h1&gt;&lt;p&gt;&lt;strong&gt;一个开源的、健全的、可操作的JavaScript Pro</summary>
      
    
    
    
    
    <category term="promise" scheme="http://example.com/tags/promise/"/>
    
  </entry>
  
  <entry>
    <title>canvas 实现手写签名中遇到的绘制偏移问题</title>
    <link href="http://example.com/2021/03/15/canvas-%E5%AE%9E%E7%8E%B0%E6%89%8B%E5%86%99%E7%AD%BE%E5%90%8D%E4%B8%AD%E9%81%87%E5%88%B0%E7%9A%84%E7%BB%98%E5%88%B6%E5%81%8F%E7%A7%BB%E9%97%AE%E9%A2%98/"/>
    <id>http://example.com/2021/03/15/canvas-%E5%AE%9E%E7%8E%B0%E6%89%8B%E5%86%99%E7%AD%BE%E5%90%8D%E4%B8%AD%E9%81%87%E5%88%B0%E7%9A%84%E7%BB%98%E5%88%B6%E5%81%8F%E7%A7%BB%E9%97%AE%E9%A2%98/</id>
    <published>2021-03-15T06:23:22.000Z</published>
    <updated>2021-03-17T08:16:31.453Z</updated>
    
    <content type="html"><![CDATA[<h1 id="canvas-实现手写签名中遇到的绘制偏移问题"><a href="#canvas-实现手写签名中遇到的绘制偏移问题" class="headerlink" title="canvas 实现手写签名中遇到的绘制偏移问题"></a>canvas 实现手写签名中遇到的绘制偏移问题</h1><p>H5实现手写签名已有非常多的网上代码不多追述。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// HTML  </span></span><br><span class="line"><span class="comment">// vue</span></span><br><span class="line">&lt;template&gt;</span><br><span class="line">&lt;!--  手写签名--&gt;</span><br><span class="line">  &lt;div&gt;</span><br><span class="line">    &lt;canvas id=<span class="string">&quot;canvas&quot;</span> width=<span class="string">&quot;320&quot;</span> height=<span class="string">&quot;150&quot;</span> ref=<span class="string">&quot;canvas&quot;</span>&gt;&lt;/canvas&gt;</span><br><span class="line">    &lt;div <span class="class"><span class="keyword">class</span></span>=<span class="string">&quot;signature-btn&quot;</span>&gt;</span><br><span class="line">      &lt;button <span class="class"><span class="keyword">class</span></span>=<span class="string">&quot;clear-btn&quot;</span> @click=<span class="string">&quot;toClear()&quot;</span>&gt;清除&lt;/button&gt;</span><br><span class="line">      &lt;button  <span class="class"><span class="keyword">class</span></span>=<span class="string">&quot;save-btn&quot;</span> @click=<span class="string">&quot;toSave()&quot;</span>&gt;保存&lt;/button&gt;</span><br><span class="line">    &lt;/div&gt;</span><br><span class="line">  &lt;/div&gt;</span><br><span class="line">&lt;/template&gt;</span><br></pre></td></tr></table></figure><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// script</span></span><br><span class="line"></span><br><span class="line"> mounted () &#123;</span><br><span class="line">    <span class="built_in">this</span>.initPage()</span><br><span class="line">  &#125;,</span><br><span class="line">  methods: &#123;</span><br><span class="line">    initPage () &#123;</span><br><span class="line">      <span class="built_in">this</span>.canvas = <span class="built_in">this</span>.$refs.canvas</span><br><span class="line">      <span class="keyword">if</span> (<span class="built_in">this</span>.canvas.getContext) &#123;</span><br><span class="line">        <span class="built_in">this</span>.ctx = <span class="built_in">this</span>.canvas.getContext(<span class="string">&#x27;2d&#x27;</span>)</span><br><span class="line">        <span class="keyword">let</span> background = <span class="string">&#x27;#ffffff&#x27;</span></span><br><span class="line">        <span class="built_in">this</span>.ctx.lineCap = <span class="string">&#x27;round&#x27;</span></span><br><span class="line">        <span class="built_in">this</span>.ctx.fillStyle = background</span><br><span class="line">        <span class="built_in">this</span>.ctx.lineWidth = <span class="number">2</span></span><br><span class="line">        <span class="built_in">this</span>.ctx.fillRect(<span class="number">0</span>, <span class="number">0</span>, <span class="number">350</span>, <span class="number">150</span>)</span><br><span class="line">        <span class="built_in">this</span>.canvas.addEventListener(<span class="string">&#x27;touchstart&#x27;</span>, <span class="function">(<span class="params">e</span>) =&gt;</span> &#123;</span><br><span class="line">          <span class="built_in">this</span>.ctx.beginPath()</span><br><span class="line">          <span class="built_in">this</span>.ctx.moveTo(e.changedTouches[<span class="number">0</span>].pageX, e.changedTouches[<span class="number">0</span>].pageY)</span><br><span class="line">        &#125;)</span><br><span class="line"></span><br><span class="line">        <span class="built_in">this</span>.canvas.addEventListener(<span class="string">&#x27;touchmove&#x27;</span>, <span class="function">(<span class="params">e</span>) =&gt;</span> &#123;</span><br><span class="line">          e.preventDefault()</span><br><span class="line">          <span class="built_in">this</span>.ctx.lineTo(e.changedTouches[<span class="number">0</span>].pageX, e.changedTouches[<span class="number">0</span>].pageY)</span><br><span class="line">          <span class="built_in">this</span>.ctx.stroke()</span><br><span class="line">        &#125;, &#123; <span class="attr">passive</span>: <span class="literal">false</span> &#125;)</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;,</span><br><span class="line">    toClear () &#123;</span><br><span class="line">      <span class="built_in">this</span>.ctx.clearRect(<span class="number">0</span>, <span class="number">0</span>, <span class="number">320</span>, <span class="number">150</span>)</span><br><span class="line">    &#125;,</span><br><span class="line">    toSave () &#123;</span><br><span class="line">      <span class="keyword">let</span> base64Img = <span class="built_in">this</span>.canvas.toDataURL()</span><br><span class="line">      <span class="built_in">this</span>.$emit(<span class="string">&#x27;toSaveSignature&#x27;</span>, base64Img)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure><h2 id="问题：canves无位移则正常绘制，放置于页面中部则绘制偏差极大，画布无结果"><a href="#问题：canves无位移则正常绘制，放置于页面中部则绘制偏差极大，画布无结果" class="headerlink" title="问题：canves无位移则正常绘制，放置于页面中部则绘制偏差极大，画布无结果"></a>问题：canves无位移则正常绘制，放置于页面中部则绘制偏差极大，画布无结果</h2><p>初始以为是鼠标点击坐标 - 画布距离页面左上角的偏移 = 实际的绘制点</p><p>但一旦滚动页面，偏移还是会出现。</p><h2 id="知识点"><a href="#知识点" class="headerlink" title="知识点"></a>知识点</h2><p><img src="http://unclealan.cn/usr/uploads/2017/11/228643359.png" alt="引用示意图"></p><p>坐标系左上角为原点，X轴向右，Y轴向下</p><h3 id="screen"><a href="#screen" class="headerlink" title="screen"></a>screen</h3><ul><li>参照点：电脑屏幕左上角</li><li>screenX：鼠标点击位置相对于电脑屏幕左上角的水平偏移量</li><li>screenY：鼠标点击位置相对于电脑屏幕左上角的垂直偏移量</li></ul><h3 id="client"><a href="#client" class="headerlink" title="client"></a>client</h3><ul><li>参照点：浏览器内容区域左上角</li><li>clientX：鼠标点击位置相对于浏览器可视区域的水平偏移量（不会计算水平滚动的距离）</li><li>clientY：鼠标点击位置相对于浏览器可视区域的垂直偏移量（不会计算垂直滚动条的距离）</li></ul><h3 id="page"><a href="#page" class="headerlink" title="page"></a>page</h3><ul><li>参照点：网页的左上角</li><li>pageX：鼠标点击位置相对于网页左上角的水平偏移量，也就是clientX加上水平滚动条的距离</li><li>pageY：鼠标点击位置相对于网页左上角的垂直平偏移量，也就是clientY加上垂直滚动条的距离</li></ul><h3 id="offset"><a href="#offset" class="headerlink" title="offset"></a>offset</h3><p>（触发事件的元素,ie,chrome支持此属性，ff不支持）</p><ul><li>offsetX：设置或获取鼠标指针位置相对于触发事件的对象的 x 坐标。 </li><li>offsetY ：设置或获取鼠标指针位置相对于触发事件的对象的 y 坐标。</li></ul><h2 id="解决"><a href="#解决" class="headerlink" title="解决"></a>解决</h2><p>鼠标事件在canvas画布上画画，使用（e.offsetX，e.offsetY）非常容易就能获取到画布上的坐标。</p><p>但是，移动端的触摸事件就不一样了，它并没有提供这两个便捷的参数，必须去计算画布上的坐标。<br>原理：【页面上的坐标】-【画布左上角的坐标】=【画布上的坐标】</p><p>获取画布坐标方法<br><code>let vertex = document.getElementById(&#39;canvas&#39;).getBoundingClientRect();</code></p><p>因为移动端页面没有水平的偏移pageX = clientX，但是有竖直的滚动条，修改为，因为vertx的值是在绘画过程中实时触发的，所以在触发事件中读取一次</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">this</span>.canvas.addEventListener(<span class="string">&#x27;touchstart&#x27;</span>, <span class="function">(<span class="params">e</span>) =&gt;</span> &#123;</span><br><span class="line">          <span class="keyword">let</span> vertex = <span class="built_in">this</span>.canvas.getBoundingClientRect()</span><br><span class="line">          <span class="built_in">this</span>.ctx.beginPath()</span><br><span class="line">          <span class="built_in">this</span>.ctx.moveTo(e.changedTouches[<span class="number">0</span>].pageX - vertex.left, e.changedTouches[<span class="number">0</span>].clientY - vertex.top)</span><br><span class="line">        &#125;)</span><br><span class="line"></span><br><span class="line">        <span class="built_in">this</span>.canvas.addEventListener(<span class="string">&#x27;touchmove&#x27;</span>, <span class="function">(<span class="params">e</span>) =&gt;</span> &#123;</span><br><span class="line">          <span class="keyword">let</span> vertex = <span class="built_in">this</span>.canvas.getBoundingClientRect()</span><br><span class="line">          e.preventDefault()</span><br><span class="line">          <span class="built_in">this</span>.ctx.lineTo(e.changedTouches[<span class="number">0</span>].pageX - vertex.left, e.changedTouches[<span class="number">0</span>].clientY - vertex.top)</span><br><span class="line">          <span class="built_in">this</span>.ctx.stroke()</span><br><span class="line">        &#125;, &#123; <span class="attr">passive</span>: <span class="literal">false</span> &#125;)</span><br></pre></td></tr></table></figure><p>为了不使绘画过程造成页面滚动影响绘制，添加组织默认动作<code> e.preventDefault()</code></p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;canvas-实现手写签名中遇到的绘制偏移问题&quot;&gt;&lt;a href=&quot;#canvas-实现手写签名中遇到的绘制偏移问题&quot; class=&quot;headerlink&quot; title=&quot;canvas 实现手写签名中遇到的绘制偏移问题&quot;&gt;&lt;/a&gt;canvas 实现手写签名中遇到的</summary>
      
    
    
    
    
    <category term="tips" scheme="http://example.com/tags/tips/"/>
    
  </entry>
  
  <entry>
    <title>eventloop</title>
    <link href="http://example.com/2021/02/23/eventloop/"/>
    <id>http://example.com/2021/02/23/eventloop/</id>
    <published>2021-02-23T09:35:08.000Z</published>
    <updated>2021-03-17T08:16:31.473Z</updated>
    
    <content type="html"><![CDATA[<h1 id="浅谈JavaScript中的Event-Loop"><a href="#浅谈JavaScript中的Event-Loop" class="headerlink" title="浅谈JavaScript中的Event Loop"></a>浅谈JavaScript中的Event Loop</h1><h2 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h2><p>众所周知，JavaScript是一门单线程的语言。准确的来说，是它的主线程是单线程，设计成单线程主要是因为JavaScript一开始是设计出来操作浏览器的Dom元素的。单线程可以规避多线程中复杂的状态同步，减轻了浏览器的渲染难度，不会出现多个线程操作同一Dom元素使得浏览器混乱。单线程也意味着，在JavaScript中同一时间只能做一件事情。但这带来一个问题，只有一个执行序列，JavaScript运行时遇到异步操作，会被阻塞。而JavaScript不能阻塞在那里等着异步操作完成，否则整个运行时都被阻塞。</p><p>那么JavaScript一门单线程的语言是怎么实现异步操作的？JavaScript运行时又是怎么感知并调用对应的回调函数呢？解决方案是依赖宿主环境的event loop机制，event loop在ES6 中没有提及，主要是靠浏览器按照HTML规范实现的，以及NodeJs实现的event loop, NodeJs与HTML规范中的event loop并不相同，今天仅谈浏览器中的event loop。</p><p>这是一个异步调用的小例子，右侧是输出的运行结果。接下来对于Event Loop的介绍能解释为什么会有这样的输出结果。 </p><h2 id="JavaScript-Runtime"><a href="#JavaScript-Runtime" class="headerlink" title="JavaScript Runtime"></a>JavaScript Runtime</h2><p> 在介绍event loop之前，我们先来介绍 JavaScript的运行时（Runtime）。从图中可以看到，JS引擎中有两大部分，一是内存堆，就是内存分配发生的地方，我们声明的变量和函数都实体存储在这里；二是函数调用栈，也就是常说的执行上下文栈，当遇到全局上下文或者函数上下文等可执行代码时，会将其推入执行栈中，JS引擎不断的执行栈顶的执行上下文，直到整个执行栈为控。JS引擎只有这唯一的解析执行JS的执行栈，这就是它的主线程。其实这样看来，JS引擎只会“傻傻”的执行栈顶函数，跟异步调用没什么关系。实际上是运行环境（runtime）去管理在什么时候压入什么函数来执行。从另一个侧面也能反映执行栈跟异步调用没什么关系，执行上下文栈是写在<a href="https://tc39.github.io/ecma262/#execution-context-stack">ECMA-262</a> 规范中，需要遵守它的是浏览器的JavaScript引擎，比如 V8、Quantum等。Event Loop的是写在 <a href="https://www.w3.org/TR/html5/webappapis.html#event-loops">HTML规范</a>中，需要遵守它的是各个浏览器，比如 Chrome、Firefox等。那么执行栈没办法处理的异步任务，会扔给浏览器里的谁来处理呢？——Web APIs。Web APIs是浏览器提供的一些额外的线程，通常由C++ 实现的，来处理非同步的Dom事件，https 请求，setTimeOut等定时器任务。既然Web APIs可以处理这些事情，为什么不让Web APIs直接把回调函数放回执行栈中呢？那这样会产生另一个问题，回调函数会随机在整个程序中出现，因为Web APIs处理完了就扔回来了。所以才又有了任务队列（这里图中写成Callback queue回调队列即放置回调函数的队列）和event loop，Web APIs在执行完毕后将回调函数推入对应的任务队列，由event loop 充当调度者的角色，去检查调用栈与任务队列。当调用栈为空了，就把一个任务推进入执行，这个执行完了，将下一个推进去。所以整体运行时过程可以总结如下：JavaScript顺序执行，遇到同步任务在主线程中，按顺序从前往后执行，如 script；遇到异步任务这种耗时复杂的操作，主线程遇到会先挂起，丢给 Web APIs找到对应线程去处理，Event loop等主线程空闲，再把已经执行完的回调拿回主线程执行。所以JS引擎的执行栈只负责取消息，event loop只负责产生消息。</p><p>这些Web APIs，task queue都是浏览器内核维护的。浏览器维护了大致五类线程，GUI渲染线程，负责浏览器的重绘、渲染等，与JS引擎线程互斥；JS 引擎线程遇到不能处理的异步任务，交给定时器线程，HTTP请求线程等处理，回调结果由事件触发线程进行接管，同时事件触发线程也负责将用户交互事件（如鼠标点击、键盘输入）放入任务队列。</p><h2 id="What-is-event-loop"><a href="#What-is-event-loop" class="headerlink" title="What is  event loop"></a>What is  event loop</h2><p>浏览器实现的event loop这个调度的大脑，到底是什么？在HTML 规范中，是这样描述event loop的：</p><p><em>To coordinate events, user interaction, scripts, rendering, networking, and so forth, user agents must use event loops as described in this section. Each agent has an associated event loop, which is unique to that agent.</em></p><p><em>（为了协调事件，用户交互，脚本，界面渲染，网络等等，宿主环境必须使用下一节描述的 event loop。每个浏览器界面都有对应的 event loop）</em></p><p>可以看到上面写的是 event loops ，我们每个界面并不止一个 event loop，而是有多个 event loop，不同的 event loop管理的方向是完全不同的，这里规范中也有相应的介绍：</p><p><em>The event loop of a similar-origin window agent is known as a window event loop. The event loop of a dedicated worker agent, shared worker agent, or service worker agent is known as a worker event loop. And the event loop of a worklet agent is known as a worklet event loop.</em></p><ul><li><p>window event loop：通常我们讲的都是这个，管理同源页面的event loop；</p></li><li><p>worker event loop：涉及到dedicated web worker（专用线程），shared web worker（共享线程），每个 worker 线程都有个与之关联的 event loop；</p></li><li><p> worklet event loop：woklet 是可以访问渲染引擎的线程，我们一般用不到</p></li></ul><p>我们接下来讲的event loop 都默认指代 window event loop，后两者我们应用并不多，worker 可以看作一个不能访问 Dom 的 JavaScript 运行环境，而 worklet 还处于草案阶段，主要应用于需要超高性能场景（worklet 中跑的是机器码而不是 JavaScript）。</p><h3 id="rules"><a href="#rules" class="headerlink" title="rules"></a>rules</h3><p>规范中介绍，Event loop 有以下属性：</p><p>1、 一个event loop可以有多个任务队列，一个任务队列是任务的集合</p><p><em>Task queues are sets, not queues, because step one of the event loop processing model grabs the first runnable task from the chosen queue, instead of dequeuing the first task.</em></p><p>规范中也说明了task，包括：HTML解析，回调、获取资源，操作Dom</p><p>2、 一个微任务队列，微任务队列不是任务队列。</p><p><em>A microtask is a colloquial way of referring to a task that was created via the queue a microtask algorithm.</em></p><p>但是在看文档时，很多对于task queue 的描述只有一个，不管是网络还是用户交互，或者计时器，都被Web APIs排进一个 task queue。但实际上规范中明确提及有多个 task queue，并举例子来解释为什么存在多个task queue。一个user agent可以有一个处理键盘鼠标事件的 task queue, 还有一个 task queue 来处理所有其他的事件。以 75% 的几率先处理鼠标和键盘的事件，这样既不会彻底不执行其他 task queues 的前提下保证用户界面的响应， 而且不会让来自同一个task queue的事件顺序错乱。task queue,虽然叫任务队列，但是其实是集合，因为event loop模型的第一步是从选择的队列中获取第一个可运行的任务，而不是第一个任务出队。</p><h3 id="task-source"><a href="#task-source" class="headerlink" title="task source"></a>task source</h3><p>Event loop 管理的是task且有多个task queue，那么是如何将任务分配进入队列的？依据就是task source——任务源。任务源就是这个任务是从哪里来的，是从addEventListener来的呢，还是从setTimeout来的。每个来自相同task source并由相同的event loop管理的task 都必须加入同一个task queue，来自不同task source 的 task 可能会排入不同的 task queue中。也就是说task queue与task source是一个对多的关系，一个task queue 中可能排列着来自不同 task sources的 tasks。为什么要这么区分呢？因为任务分贵贱，比如键盘和鼠标事件，就要把它的响应优先级提高，以便尽可能的提高网页浏览的用户体验。但是具体什么task source对应什么 task queue，规范并没有具体说明。但规范对通用task source 进行了介绍：DOM 操作任务源；用户交互任务源，比如鼠标键盘的操作；网络请求任务源；历史浏览任务源，将history.back 等api 排入 task queue。</p><h3 id="运行过程"><a href="#运行过程" class="headerlink" title="运行过程"></a>运行过程</h3><p>现在我们可以具体来看看event loop是怎么执行的。Event loop会在整个页面存在时不停的执行以下步骤：</p><ol><li><p>选取一个有可执行任务的task queue，user agent可以选择任何task队列，如果没有可选的task queue，则跳到下边的microtasks步骤。 </p></li><li><p>在task queue中选择oldestTask，将其设置为正在运行的task。 </p></li><li><p>Run: 运行被选择的task。 </p></li><li><p>将Event Loop的current running task变为null。 </p></li><li><p>从task队列里移除前边运行的task。</p></li><li><p>Microtasks: 执行Microtasks任务检查（也就是执行microtasks队列里的任务） </p></li><li><p>更新渲染（Update the rendering）：可以简单理解为浏览器渲染</p></li><li><p>如果接下来没有要执行的 task/microstask，event loop 将会计算空闲时间。</p></li><li><p>返回到第一步。</p></li></ol><p>这里会有一点疑惑，前文说到task queue不是队列是集合，为什么还会出现选择oldestTask的情况？个人理解是取可执行任务中oldestTask。那我们再来看看微任务检查点做了什么：</p><p>当user agent去执行一个microtask checkpoint，如果microtask checkpoint的flag（标识）为false，user agent必须运行下面的步骤：</p><p>1.将microtask checkpoint的flag设为true。</p><p>2.Microtask queue handling: 如果event loop的microtask队列为空，直接跳到第八步（Done）。</p><p>3.在microtask队列中选择最老的一个任务。</p><p>4.将上一步选择的任务设为event loop的currently running task。</p><p>5.运行选择的任务。</p><p>6.将event loop的currently running task变为null。</p><p>7.将前面运行的microtask从microtask队列中删除，然后返回到第二步（Microtask queue handling）。</p><p>8.Done: 每一个environment settings object它们的 responsible event loop就是当前的event loop，会给environment settings object发一个 rejected promises 的通知。</p><p>9.清理IndexedDB的事务。</p><p>10.将microtask checkpoint的flag设为false。</p><p>从上述运行过程可以看出，当微任务队列的标记被写为true之后，只要microtask的队列不为空，event loop中的当前任务就会按顺序执行microtask队列中的所有任务。而任务队列是每次仅执行一个任务而微任务microtask队列是独立的一个队列，在event loop执行过程中才进入到任务队列task queue一次执行。</p><p>上述这么多步骤，简单来说event loop在一直进行宏任务-微任务-宏任务的过程，在执行完微任务后，可能会进行浏览器的渲染。</p><h3 id="What-is-tasks"><a href="#What-is-tasks" class="headerlink" title="What is  tasks"></a>What is  tasks</h3><p>那么什么是宏任务？在规范中其实并没有提到marcotask的概念，仅有task的描述。在其他文献中找到这样一句解释：<em>One go-around of the event loop will have exactly one task being processed from the macrotask queue (this queue is simply called the task queue in the WHATWG specification).</em></p><p>既然每个event loop都必须有一个来自宏任务队列的任务在执行，那微任务又怎么说呢？这就跟之前提到的微任务队列是独立于任务队列有关，整个任务可以理解为，所有的宏任务放在一个宏任务队列（即任务队列），处理完一个宏任务(从sccript开始)，将微任务队列（包含当时所有的微任务）压入任务队列（宏任务队列）并执行，之后再取下一个任务队列（宏任务）中的宏任务。通常被认为是宏任务的有：</p><p>•    script（整体代码）</p><p>•    setTimeout</p><p>•    setInterval</p><p>•    setImmediate</p><p>•    I/O</p><p>•    UI rendering</p><h3 id="What-is-microtask"><a href="#What-is-microtask" class="headerlink" title="What is microtask"></a>What is microtask</h3><p>那么微任务又是什么？初始不能被放入任务队列的就是微任务，微任务分两类：回调微任务跟复合微任务，通常只会碰到回调微任务。微任务的也没有具体的分类，通常我们把以下任务认为是微任务：</p><p>•    process.nextTick（Node.js 环境）</p><p>•    Promise（这里指浏览器实现的原生 Promise）</p><p>•    Object.observe（已被 MutationObserver 替代）</p><p>•    MutationObserver</p><p>•    postMessage</p><p>我们习惯把宏任务和微任务都理解成 JavaScript 异步执行的一种形式。事实上只有宏任务是异步的，而微任务是对宏任务代码执行顺序的再分配；宏任务执行完后总是会执行完所有微任务，这种意义上微任务是阻塞主线程的，如果你在某个微任务中不断创建新的微任务，毫无疑问界面会出现假死。所有微任务的意义在于执行一些总是想在某个任务完成后再执行的代码。</p><h3 id="summary"><a href="#summary" class="headerlink" title="summary"></a>summary</h3><p>现在对宏任务中的具体函数在进行一点补充，setInterval和setTimeout类似，只是前者是循环的执行。对于执行顺序来说，setInterval会每隔指定的时间将注册的函数置入Event Queue，如果前面的任务耗时太久，那么同样需要等待。setTimeout(fn，0)表示当主线程执行栈为空时，立即执行该函数，即实际执行时间会比预计的长。setInterval(fn, ms)我们已经知道不是每过ms秒会执行一次fn，而是每过ms秒，会有fn进入task Queue。一旦setInterval的回调函数fn执行时间超过了延迟时间ms，那么就完全看不出来有时间间隔了。</p><p>微任务中经常会碰到的就是Promise，这里简单介绍一点，Promise其实是一个异步容器，构造函数的参数函数是完完全全的同步代码，只有状态改变触发的then、catch回调才是异步代码，   它只关心状态改变后的异步执行，并且承诺给你一个稳定的结果，嵌套Promise,这也有一个小例子：</p><p>这是一个四层嵌套的Promise，因为最里层的Promise需要等待几秒才resolve，所以最外层的Promise返回的实例也要等待几秒才会打印日志。也就是说，只有最里层的Promise状态变成fulfilled，最外层的Promise状态才会变成fulfilled。</p><p>Async/await是一个Promise语法糖，await 实际上是一个让出线程的标志。每次使用 await, 解释器都创建一个 promise 对象，然后把剩下的 async 函数中的操作放到 then 回调函数中。await后面的表达式会先执行一遍，将await后面的代码加入到microtask中，然后就会跳出整个async函数来执行后面的代码。</p><p>现在在介绍了event loop后可以看下最开始的例子的结果是什么顺序执行出来的。</p><p>首先，全局代码作为宏任务进入执行栈，setTimeout为宏任务，先加入宏任务队列，Promise为微任务，也加入微任务队列。在第一个宏任务执行完毕后，近输出了两个同步代码：script start，script end。</p><table><thead><tr><th><strong>Task queue</strong></th><th><strong>Microtask queue</strong></th></tr></thead><tbody><tr><td>setTimeout</td><td>Promise.then</td></tr></tbody></table><p>接下来，将微任务取出执行，同时产生的新的微任务，在此次执行中一并处理，顺序输出：promise1，promise2</p><table><thead><tr><th><strong>Task queue</strong></th><th><strong>Microtask queue</strong></th></tr></thead><tbody><tr><td>setTimeout</td><td>Process.then</td></tr><tr><td></td><td>Promise.then2</td></tr></tbody></table><p>最后执行下一个宏任务setTimeout，输出setTimeout</p><table><thead><tr><th><strong>Task queue</strong></th><th><strong>Microtask queue</strong></th></tr></thead><tbody><tr><td>setTimeout</td><td></td></tr></tbody></table><h2 id="EventLoop与浏览器渲染"><a href="#EventLoop与浏览器渲染" class="headerlink" title="EventLoop与浏览器渲染"></a>EventLoop与浏览器渲染</h2><p>最后来简单介绍一点event loop与浏览器渲染、帧动画以及空闲回调的内容。还是有四个问题可以先思考一下：</p><p>1、每一轮 Event Loop 都会伴随着渲染吗？</p><p>2、resize、scroll 这些事件是何时去派发的？</p><p>3、requestAnimationFrame 在哪个阶段执行,在渲染前还是后？在 microTask 的前还是后？</p><p>4、requestIdleCallback 在哪个阶段执行？如何去执行？在渲染前还是后？在 microTask 的前还是后？</p><p>关于第一个问题，规范中也介绍了event loop每一轮执行后进行渲染的标准，宏任务执行完毕，微任务队列也被清空后，进入更新渲染阶段，这里有一个“rendering opportunity”的概念，也就是说不一定每一轮 event loop都会对应一次浏览器渲染，要根据屏幕刷新率、页面性能、页面是否在后台运行来共同决定，通常来说这个渲染间隔是固定的。所以多个task很可能在一次渲染之间执行。</p><p>浏览器会尽可能的保持帧率稳定，例如页面性能无法维持 60fps（每 16.66ms 渲染一次）的话，那么浏览器就会选择 30fps 的更新速率，而不是偶尔丢帧。如果浏览器上下文不可见，那么页面会降低到 4fps 左右甚至更低。</p><p>如果满足以下条件，也会跳过渲染：</p><ol><li><p>浏览器判断更新渲染不会带来视觉上的改变。</p></li><li><p>“map of animation frame callbacks”为空，也就是帧动画回调为空，可以通过 “requestAnimationFrame”来请求帧动画。</p></li></ol><p>如果浏览器判断本轮需要进行渲染，则进行下面几步：</p><p>•    对于需要渲染的文档，如果窗口的大小发生了变化，执行监听的 resize 方法。</p><p>•    对于需要渲染的文档，如果页面发生了滚动，执行 scroll 方法。</p><p>•    对于需要渲染的文档，执行帧动画回调，也就是 requestAnimationFrame 的回调。</p><p>•    对于需要IntersectionObserver 的回调。</p><p>•    对于需要渲染的文档，重新渲染绘制用户界面。</p><p>•    判断 task队列和microTask队列是否都为空，如果是的话，则进行 Idle 空闲周期的算法，判断是否要执行 requestIdleCallback 的回调函数。</p><p>从这个过程可以看出，对于resize 和 scroll来说，并不是到了这一步才去执行滚动和缩放，那岂不是要延迟很多？浏览器当然会立刻帮我们滚动视图，根据CSSOM 规范所讲，浏览器会保存一个 pending scroll event targets，等到事件循环中的 scroll这一步，去派发一个事件到对应的目标上，驱动它去执行监听的回调函数而已。resize也是同理。</p><p>在Web应用中，实现动画效果的方法比较多，Javascript 中可以通过定时器 setTimeout 来实现，css3 可以使用 transition 和 animation 来实现，html5 中的 canvas 也可以实现。除此之外，html5 还提供一个专门用于请求动画的API，那就是 requestAnimationFrame，顾名思义就是请求动画帧。requestAnimationFrame是浏览器用于定时循环操作的一个接口，类似于setTimeout，主要用途是按帧对网页进行重绘。</p><ul><li><p>在重新渲染前调用。官方推荐的用来做一些流畅动画所应该使用的 API</p></li><li><p>系统来决定回调函数的执行时机，保证回调函数在屏幕每一次的刷新间隔中只被执行一次</p></li></ul><p>为什么要在重新渲染前去调用？因为requestAnimationFrame是官方推荐的用来做一些流畅动画所应该使用的 API，做动画不可避免的会去更改DOM，而如果在渲染之后再去更改 DOM，那就只能等到下一轮渲染机会的时候才能去绘制出来了，这显然是不合理的。requestAnimationFrame在浏览器决定渲染之前给你最后一个机会去改变 DOM 属性，然后很快在接下来的绘制中帮你呈现出来，所以这是做流畅动画的不二选择。</p><p>相比较setTimeout的缺陷：</p><ul><li><p>setTimeout的执行时间并不是确定的，实际执行时间会比设定时间晚一些</p></li><li><p>刷新频率受屏幕分辨率和屏幕尺寸的影响，因此不同设备的屏幕刷新频率可能会不同，而 setTimeout只能设置一个固定的时间间隔，这个时间不一定和屏幕的刷新时间相同</p></li></ul><p>requestAnimationFrame由系统来决定调用渲染的时间，相比setTimoeout更加可靠。</p><p>现在来看一个闪烁动画的小例子，假设我们现在想要快速的让屏幕上闪烁 红、蓝两种颜色，保证用户可以观察到，如果我们用 setTimeout 来写，并且带着我们长期的误解宏任务之间一定会伴随着浏览器绘制，那么你会得到一个预料之外的结果。</p><p>可以看出这个结果是非常不可控的，如果这两个 Task 之间正好遇到了浏览器认定的渲染机会，那么它会重绘，否则就不会。由于这俩宏任务的间隔周期太短了，所以很大概率是不会的。如果你依赖这个 API 来做动画，那么就很可能会造成「掉帧」。</p><p>接下来我们换成requestAnimationFrame试试？我们用一个递归函数来模拟 10 次颜色变化的动画。浏览器会非常规律的把这 10 组也就是 20 次颜色变化绘制出来。</p><p>这里是requestAnimationFrame的浏览器兼容情况，如果在开发中有场景需要这个功能，可以考虑使用这个API。</p><p>requestIdleCallback是浏览器提供给我们的空闲调度算法，关于它的简介可以看 MDN 文档，意图是让我们把一些计算量较大但是又没那么紧急的任务放到空闲时间去执行。不要去影响浏览器中优先级较高的任务，比如动画绘制、用户输入等等。</p><p>当渲染有序进行，这种有序的 “浏览器 -&gt; 用户 -&gt; 浏览器 -&gt; 用户”的调度基于一个前提，就是我们要把任务切分成比较小的片，不能说浏览器把空闲时间让给你了，你去执行一个耗时“10s”的任务，那肯定也会把浏览器给阻塞住的。这就要求我们去读取requestIdleCallback提供给你的deadline里的时间，去动态的安排我们切分的小任务。浏览器信任了你，你也不能辜负它呀。</p><p>当渲染长期空闲，可能在几帧的时间内浏览器都是空闲的，并没有发生任何影响视图的操作，它也就不需要去绘制页面，这种情况下还是会有 <code>50ms</code> 的 <code>deadline</code> 呢？是因为浏览器为了提前应对一些可能会突发的用户交互操作，比如用户输入文字。如果给的时间太长了，你的任务把主线程卡住了，那么用户的交互就得不到回应了。50ms 可以确保用户在无感知的延迟下得到回应。</p><p>这里可以做一个小实验：这个动画的例子很简单，就是利用在requestAnimationFrame每帧渲染前的回调中把方块的位置向右移动 10px。最后加了一个 requestIdleCallback 的函数，回调里会 alert(‘rIC’)，来看一下演示效果：alert 在最开始的时候就执行了，为什么会这样呢一下，想一下「空闲」的概念，我们每一帧仅仅是把 left 的值移动了一下，做了这一个简单的渲染，没有占满空闲时间，所以可能在最开始的时候，浏览器就找到机会去调用 rIC 的回调函数了。</p><p>我们简单的修改一下 step 函数，在里面加一个很重的任务，1000 次循环打印。</p><p>其实和我们预期的一样，由于浏览器的每一帧都”太忙了”,导致它真的就无视我们的 rIC 函数了。</p><p>如果给 rIC 函数加一个 timeout 呢：浏览器会在大概 500ms 的时候，不管有多忙，都去强制执行 rIC 函数，这个机制可以防止我们的空闲任务被“饿死”。</p><p>最后总结一下：</p><p>事件循环不一定每轮都伴随着重渲染，但是一定会伴随着微任务执行。</p><p>决定浏览器视图是否渲染的因素很多，浏览器是非常聪明的。</p><p>requestAnimationFrame在重新渲染屏幕之前执行，非常适合用来做动画。</p><p>requestIdleCallback在渲染屏幕之后执行，并且是否有空执行要看浏览器的调度，如果你一定要它在某个时间内执行，请使用 timeout参数。</p><p>resize和scroll事件其实自带节流，它只在 Event Loop 的渲染阶段去执行事件。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;浅谈JavaScript中的Event-Loop&quot;&gt;&lt;a href=&quot;#浅谈JavaScript中的Event-Loop&quot; class=&quot;headerlink&quot; title=&quot;浅谈JavaScript中的Event Loop&quot;&gt;&lt;/a&gt;浅谈JavaScript中的</summary>
      
    
    
    
    
  </entry>
  
</feed>
